@model IEnumerable<goldberg_MIS4200.Models.Organization>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.orgName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.orgMajorAssociated)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.orgName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.orgMajorAssociated)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.organizationId }) |
            @Html.ActionLink("Details", "Details", new { id=item.organizationId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.organizationId })
        </td>
    </tr>
}

</table>
